<p id="notice"><%= notice %></p>

<h1>Chirps</h1>

<ul>
  <% @chirps.each_with_index do |chirp, index| %>
    <li><%= index + 1 %> -- <%= chirp.text.upcase %> -- <%= chirp.upvote %> upvotes | <%= link_to 'Upvote', upvote_chirp_path(chirp.id), method: :put %> | <%= link_to 'Destroy', chirp, method: :delete %></li>
  <% end %>
</ul>

<%= link_to 'New Chirp', new_chirp_path %>